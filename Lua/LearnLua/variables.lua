-- Variables in Lua

-- local - By default variables are globally scoped, you can write 'local' to make them local.
local number = 1

local word = "hello"
local word2 = 'hello again' -- strings can also be written with single quotes.
local multiLineString = [[This is a 
multi-line string]]

local isCool = true

local nothing = nil -- null = nil in lua

local var1, var2 = 0, 1 -- you can declare multiple variables on the same line.